/**
 * Build script for Vercel API functions (DEPRECATED)
 *
 * NOTE: This script is deprecated as we're now using the Hono server directly
 * from src/server/vercel.ts for deployment. Vercel will handle the bundling.
 *
 * This script is kept for backward compatibility but will be removed in a future release.
 */

console.log('⚠️  DEPRECATED: API build script is no longer needed');
console.log('ℹ️  Vercel will now use src/server/vercel.ts directly');
console.log('🔄 Please update your deployment configuration if needed');

// Keep the script for backward compatibility
process.exit(0);
