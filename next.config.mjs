/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Adicione outras configurações do Next.js aqui, se necessário.
  // Por exemplo, para habilitar o diretório src se não for o padrão:
  // experimental: {
  //   appDir: true, // Já é padrão em versões mais recentes
  // },
  // typescript: {
  //   ignoreBuildErrors: true, // Se você tiver problemas de tipo temporários durante a migração
  // }
};

export default nextConfig;
